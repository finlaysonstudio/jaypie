#!/bin/bash

# Capture both stdout and stderr
output=$("$@" 2>&1)

# Check if output is empty
if [ -z "$output" ]; then
    echo "No output to process"
else
    echo "Running \`$@\` produces the following output:"
    echo "$output"
fi

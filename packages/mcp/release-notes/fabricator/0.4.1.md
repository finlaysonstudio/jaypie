---
version: 0.4.1
date: 2026-08-02
summary: Drop the now-redundant no-redeclare disable comment
---

## Changes

- Remove the file-level `/* eslint-disable no-redeclare */` from
  `src/index.ts`. `@jaypie/eslint` 1.2.6 turns the core rule off for
  TypeScript, where it cannot read an overload and `tsc` already rejects a
  genuine redeclaration.

No functional change.

---
version: 0.3.3
date: 2026-06-28
summary: Update the uuid and @faker-js/faker dependencies
---

## Changes

- Update the `uuid` dependency from `^13.0.0` to `^14.0.1`. The functions in use
  (`v5`, `parse`) are unchanged across the major, so `uuidFrom`, `uuidToBytes`,
  `uuidToNumber`, and seeding behavior are unaffected.
- Update `@faker-js/faker` from `^9.3.0` to `^10.5.0`. The deterministic seeding
  contract holds; faker v10 requires Node `^20.19 || ^22.13 || ^23.5 || >=24`.

---
version: 1.2.56
date: 2026-06-24
summary: Re-export parseFrontmatter and stringifyFrontmatter from @jaypie/kit
---

## Changes

- Picks up `@jaypie/kit@^1.2.11`, which adds `parseFrontmatter` and
  `stringifyFrontmatter`. These are re-exported from `jaypie` (via
  `export * from "@jaypie/kit"`), so `import { parseFrontmatter } from "jaypie"`
  now works.

No runtime behavior changes.

---
version: 1.2.75
date: 2026-08-01
summary: Re-export secret and isSecret; pick up logger redaction and lambda log options
---

## Changes

- Re-exports `secret` and `isSecret` from `@jaypie/logger`
- Picks up `@jaypie/logger` 1.2.22 (recursive redaction) and `@jaypie/lambda` 1.2.13 (`logEvent`/`logResponse` options)

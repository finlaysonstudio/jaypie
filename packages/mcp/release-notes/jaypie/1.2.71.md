---
version: 1.2.71
date: 2026-07-26
summary: Re-export loadEnvVariables; handlers hydrate non-secret variables
---

## Features

- Picks up `@jaypie/aws` 1.2.11, `@jaypie/lambda` 1.2.12, and
  `@jaypie/express` 1.2.29.
- `loadEnvVariables` and `clearEnvVariablesCache` are re-exported.
- Every handler hydrates the non-secret `variables` bundle written by the
  `variables` prop on `JaypieLambda`.

Addresses issue #443.

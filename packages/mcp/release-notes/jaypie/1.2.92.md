---
version: 1.2.92
date: 2026-08-30
summary: Pick up the Datadog fabric service and the expressHandler logging options
---

## Changes

- `@jaypie/datadog` 1.2.7 — `datadogService` fabric service, `getEnvSecret` key
  resolution, Jaypie errors from the service.
- `@jaypie/express` 1.2.36 — `sensitiveHeaders` and `logBody` options on
  `expressHandler` and `expressStreamHandler`.

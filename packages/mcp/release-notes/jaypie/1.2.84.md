---
version: 1.2.84
date: 2026-08-11
summary: Pull @jaypie/errors 1.2.5 so every error class accepts a cause
---

## Changes

- `@jaypie/errors` range moves to `^1.2.5`, which accepts `cause` on every
  error class. Rethrowing a caught error as a Jaypie error now preserves the
  chain.

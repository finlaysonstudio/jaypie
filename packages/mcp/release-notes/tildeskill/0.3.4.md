---
version: 0.3.4
date: 2026-08-30
summary: Derive rollup externals from package.json so a new dependency cannot drift out of the list
---

## Changes

- `rollup.config.js` builds `external` from `dependencies` and
  `peerDependencies` plus `/^node:/`, replacing a hand-maintained array.
  `@jaypie/fabric` and `@jaypie/kit` had been added as dependencies without
  reaching that array, so both warned as unresolved on every build.
- Drops the stale `gray-matter` entry, which the package neither depends on nor
  imports.
- Emitted output is unchanged.

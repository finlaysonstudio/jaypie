---
version: 1.2.69
date: 2026-09-05
summary: Mock log reports sessionActive
---

## Changes

- `mockLogFactory` sets `sessionActive: true` on the mock log, matching
  `@jaypie/logger` 1.2.24, so code guarding a tally on the session runs under
  test
- Tests exercising the unguarded path set `log.sessionActive = false`

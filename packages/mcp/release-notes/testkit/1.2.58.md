---
version: 1.2.58
date: 2026-07-20
summary: Mock resolveTtl and per-call ttl on the dynamodb create/update mocks
---

## Changes

- Export `resolveTtl` and `DEFAULT_TTL_ATTRIBUTE` from the `@jaypie/dynamodb` mock.
- The `createEntity`/`updateEntity` mocks accept a per-call `ttl` (`TtlInput | false`) and apply it to the returned entity, matching the real create/update behavior.

---
version: 1.2.67
date: 2026-08-30
summary: Mock the new @jaypie/datadog exports
---

## Changes

- `@jaypie/testkit/mock` passes through `datadogService` and `DATADOG_HELP` from
  `@jaypie/datadog`. The fabric service is passed through rather than stubbed:
  its command router is the behavior under test whenever a consumer registers it
  as a tool.

---
version: 1.2.77
date: 2026-09-23
summary: dynamodb mocks accept preserveTimestamps and the indexEntity options object
---

## Changes

- `indexEntity` mock takes `IndexEntityOptions` (`{ preserveTimestamps?, suffix? }`).
- `createEntity` and `updateEntity` mocks pass `preserveTimestamps` through;
  `transactWriteEntities` accepts it.

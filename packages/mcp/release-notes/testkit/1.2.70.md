---
version: 1.2.70
date: 2026-09-10
summary: Remove the orphaned placeholders copy; the mock wraps @jaypie/kit 1.2.19
---

## Changes

- Removed `src/placeholders.ts` and `src/types/placeholders.d.ts`. Neither was
  imported nor built, and the copy carried the falsy-value bug fixed in
  `@jaypie/kit` 1.2.19.
- The `placeholders` mock continues to wrap the real `@jaypie/kit`
  implementation, so it picks up the fix.

---
version: 1.2.75
date: 2026-09-18
summary: Mock Llm.question and the TypeSafe provider
---

## Changes

- `Llm.question` (static, instance, and on the inner provider) resolves a
  mock question response with one `choice` answer and `emulated: false`.
- New mocks: `TypeSafeProvider`, `TypeSafeClient`, `LlmQuestionType`,
  `normalizeDistribution`, and `peakConfidence`.

---
version: 1.3.28
date: 2026-08-11
summary: LlmError routes its cause through JaypieError
---

## Changes

- `LlmError` passes `cause` to `super` rather than declaring and assigning its
  own field, now that `@jaypie/errors` 1.2.5 accepts the option. The provider
  error is still preserved on `.cause`.

No functional change.

---
version: 1.3.41
date: 2026-09-07
summary: Repoint MODEL.FIREWORKS.DEEPSEEK at the dated deepseek-v4-pro-0813 id
---

## Changes

- `MODEL.FIREWORKS.DEEPSEEK` is now
  `accounts/fireworks/models/deepseek-v4-pro-0813`. Fireworks renamed the model;
  the undated `accounts/fireworks/models/deepseek-v4-pro` stopped responding
  entirely, which is why the live capability matrix excluded it on 2026-08-31
- `COST` prices the new id at $1.32 input, $3.96 output, $0.044 cached input
  read per million tokens (Fireworks Standard tier). The retired undated id
  keeps its `COST` entry per policy, so historical tallies still resolve
- The model runs the live capability matrix again; `minimax-m2p7` stays excluded

---
version: 1.3.46
date: 2026-09-21
summary: MODEL.GROK names grok-4.7
---

## Changes

- `MODEL.GROK` and `PROVIDER.XAI.DEFAULT` name `grok-4.7`, superseding `grok-4.6`
- `COST["grok-4.7"]` prices the model at $2.00 input, $6.00 output, and $0.50 cache read per million tokens, matching what xAI publishes on the models endpoint
- `COST["grok-4.6"]` remains so historic usage records stay replayable

## Testing

- Live, 2026-09-21 (`APP_MODELS=grok-4.7 npm run test:llm:matrix`): all seven capability cells passed — plain, tools, structured, both, pdf, image, and temperature

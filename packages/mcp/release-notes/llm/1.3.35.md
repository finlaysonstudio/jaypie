---
version: 1.3.35
date: 2026-09-02
summary: MODEL.FABLE points at claude-fable-5-1
---

## Changes

- `MODEL.FABLE` is `claude-fable-5-1`, superseding `claude-fable-5` in the same
  tier. Every `MODEL.FABLE` consumer follows, including the live capability
  matrix and the Anthropic hot tests.
- `COST` gains `claude-fable-5-1` at $10 input / $50 output per million tokens,
  with 5-minute cache writes at $12.50 and 1-hour at $20. Cache reads are
  $0.25, a quarter of the `claude-fable-5` rate. The `claude-fable-5` entry
  stays in place so historic usage records remain replayable.

## Testing

- `determineModelProvider` resolves the id on the existing `fable` match word.
- `maxOutputTokens` resolves 128K on the existing `claude|fable|mythos` pattern.
- `supportsAnthropicEffort` parses major 5 / minor 1 and sends
  `output_config.effort`.

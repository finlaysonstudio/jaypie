---
version: 1.3.34
date: 2026-08-30
summary: Externalize @aws-sdk/client-bedrock-runtime so the build stops warning it is unresolved
---

## Changes

- `rollup.config.js` adds `@aws-sdk/client-bedrock-runtime` to `external`. The
  SDK is an optional peer loaded through a dynamic import, so rollup was warning
  it could not resolve it on every build.
- Emitted output is unchanged: rollup already treated the specifier as external,
  it simply warned while doing so.

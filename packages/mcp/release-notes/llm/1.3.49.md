---
version: 1.3.49
date: 2026-09-24
summary: COST prices nemotron-lightning-3p5-30b-a3b; the model is not cataloged because it does not support tools or structured output
---

## Changes

- `COST` prices `accounts/fireworks/models/nemotron-lightning-3p5-30b-a3b` at $0.05 input, $0.20 output, $0.01 cache read per million tokens (Fireworks standard serverless rates)
- `MODEL.FIREWORKS.NEMOTRON_LIGHTNING` is deliberately absent. The model does not support tools or structured output: a `json_schema` `response_format` hangs with no response, and after a tool call it answers in prose. A caller can still pass the id directly
- `MODEL.FIREWORKS.NEMOTRON` remains `nemotron-3-ultra-nvfp4`, the newest serverless Nemotron Ultra

## Testing

- `npm run test -w packages/llm`: unit suite passes; Google hot specs failed on provider "high demand" 503s
- Live, 2026-09-24 (`APP_MODELS=accounts/fireworks/models/nemotron-lightning-3p5-30b-a3b npm run test:llm:matrix`, twice): plain and temperature passed; tools, structured, and both timed out at 180s

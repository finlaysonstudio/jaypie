---
version: 2.0.1
date: 2026-08-21
summary: Standalone npm test works in the package directory
---

## Changes

- Add `vite.config.ts` so `npm test -w packages/repokit` resolves against the
  package rather than the root `projects` glob, which pointed at directories
  that do not exist relative to the package.

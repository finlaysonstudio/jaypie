---
version: 0.8.112
date: 2026-07-26
summary: Document the variables prop and loadEnvVariables
---

## Documentation

- `skill("variables")` documents the `variables` prop, the hydration rules,
  and the module-scope timing caveat.
- `skill("aws")` documents `loadEnvVariables` and `clearEnvVariablesCache`.
- `skill("cdk")` and `skill("handlers")` reference the new path.

Addresses issue #443.

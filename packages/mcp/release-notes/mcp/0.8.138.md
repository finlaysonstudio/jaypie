---
version: 0.8.138
date: 2026-08-23
summary: Document loop-stop reasons and id-embedded GitHub OIDC subjects
---

## Changes

- `llm` skill documents `LlmResponseErrorReason` and the two loop stops
  (`max_turns`, `tool_errors`).
- `cicd-environments` skill documents GitHub's id-embedded OIDC subject, the
  two-pattern trust policy, and the `Not authorized to perform
  sts:AssumeRoleWithWebIdentity` failure it causes.

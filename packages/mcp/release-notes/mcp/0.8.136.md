---
version: 0.8.136
date: 2026-08-21
summary: The errors skill documents instanceof across module formats
---

## Changes

- The errors skill states that `instanceof` is the supported check for Jaypie
  error types, that it holds across the ESM and CommonJS builds and across
  duplicate installs, and that class identity (`===`) does not (#502).

---
version: 0.8.105
date: 2026-07-22
summary: Document the unified model catalog, Nova pricing, and widened Bedrock matching in the llm skill
---

## Changes

- `skills/llm.md` documents `MODEL.NOVA_PRO` / `MODEL.NOVA_LITE`, `MODEL.FIREWORKS.GPT_OSS`, and the retirement of Fireworks Nemotron.
- Records that `MODEL.*` is the single source of truth for CI coverage: the live capability matrix derives from it and the workflow shards it by provider.
- Adds the widened Bedrock match words (`deepseek.`, `google.gemma`, `moonshotai.`, `openai.gpt-oss`) to the provider table.
- Clarifies pricing: Nova is priced at the standard US on-demand rate with a published `0` cache write, while gateway routes (`MODEL.OPENROUTER.*` and third-party Bedrock ids) stay unpriced.
- Corrects the Gemini default model to `gemini-3.6-flash`.

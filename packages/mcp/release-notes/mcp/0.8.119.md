---
version: 0.8.119
date: 2026-08-01
summary: Document log redaction and lambda logEvent/logResponse options in skills
---

## Changes

- `skill("logs")` documents the redaction layers (name denylist with renders, value heuristics, `secret()` marking) and `LOG_REDACT`/`LOG_REDACT_KEYS` configuration
- `skill("lambda")` and `skill("logs")` document the `logEvent`/`logResponse` handler options

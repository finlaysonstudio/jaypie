---
version: 0.8.171
date: 2026-09-24
summary: logs skill describes report() recursive merge and debug-level leaf overwrite
---

## Changes

- **`logs` skill**: `log.report()` merges plain objects recursively and logs
  a changed leaf at debug, not warn; `log.tally()` is for values that
  accumulate (#586).

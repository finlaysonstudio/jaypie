---
version: 0.8.103
date: 2026-07-20
summary: Document DynamoDB TTL support in the dynamodb skill
---

## Changes

- Document TTL support (`resolveTtl`, model default `ttl`, per-call `ttl` on create/update, and `JaypieDynamoDb` default TTL) in the `dynamodb` skill, plus release notes for `@jaypie/dynamodb` 0.6.9, `@jaypie/fabric` 0.3.6, `@jaypie/constructs` 1.2.71, and `@jaypie/testkit` 1.2.58.

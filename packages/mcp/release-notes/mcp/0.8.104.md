---
version: 0.8.104
date: 2026-07-21
summary: Document LLM.COST model pricing in the llm skill
---

## Changes

- `skills/llm.md` gains a "Model Pricing" section covering `LLM.COST` and the `LlmModelCost` field table, with a worked per-million-token cost calculation.
- Documents why keys are literal model ids rather than `MODEL.*` references (retired models keep their price), and why Bedrock and `MODEL.OPENROUTER.*` are deliberately unpriced (both resell many vendors and price per backend route).

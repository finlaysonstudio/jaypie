---
version: 0.8.109
date: 2026-07-25
summary: Vocabulary skill defines message and job, reserves session, adds reservation hygiene and the re-earn test, discourages thread
---

## Changes

- Vocabulary: new Reservation Hygiene section (magnetic-not-bound principle, re-earn test)
- Vocabulary: define `message` as the emission genus with category-selected disposition (`status` lifecycle vs diagnostic `level`)
- Vocabulary: define `job` (a run of a plan; default run lifecycle `status` vocabulary)
- Vocabulary: reserve `session` (bounded, resumable scope; declared `active | idle | closed` lifecycle; crosses rather than nests with `case`)
- Vocabulary: discourage `thread` (=> `session`, `scope`); `conversation` is informal for the collection, never a model

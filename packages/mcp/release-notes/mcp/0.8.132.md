---
version: 0.8.132
date: 2026-08-11
summary: The errors skill documents cause and drops a constructor option that never existed
---

## Changes

- The errors skill documented an "Error Context" option
  (`new NotFoundError(message, { context: {...} })`) that no error class ever
  accepted. Replaced with the `cause` option added in `@jaypie/errors` 1.2.5,
  and the file's test example is corrected to match.

---
version: 0.8.156
date: 2026-09-13
summary: Skills document index Query and Scan grants for tables passed to JaypieLambda and JaypieMigration
---

## Changes

- **`cdk`, `dynamodb`, and `migrations` skills** document that tables passed
  via `tables` get `Query` and `Scan` on `${tableArn}/index/*`, covering
  indexes created by migrations that CDK never declares (#546).

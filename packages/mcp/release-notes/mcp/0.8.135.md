---
version: 0.8.135
date: 2026-08-20
summary: The web skill documents zoneless distributions
---

## Changes

- The web skill states that the CloudFront distribution is unconditional, that
  only the certificate and the alias record require a `host` and `zone`, and
  that `spa: true` works before DNS is wired (#500).

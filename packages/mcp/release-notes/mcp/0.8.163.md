---
version: 0.8.163
date: 2026-09-19
summary: Document Llm.ocr and the LlamaCloud provider in the llm skill
---

## Changes

### Documentation

- `skills/llm.md` gains an "OCR" section: the document forms `Llm.ocr`
  accepts, the page and usage shape, `pages`/`tables`/`images`/`timeout`/
  `providerOptions`, per-tier LlamaParse versions, and fallback across
  engines.
- The providers table carries a `llamacloud` row, the model constants name
  `MODEL.LLAMAPARSE.*` and `PAGE_COST`, and the environment block carries
  `LLAMA_CLOUD_API_KEY`.

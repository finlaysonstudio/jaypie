---
version: 1.2.23
date: 2026-09-03
summary: Log an overwritten report key at debug instead of warn
---

## Changes

- `log.report()` logs `Overwriting report key` at debug

Overwriting a report key is an ordinary update, not a condition that needs attention. Warn is reserved for a request running other than as expected.
